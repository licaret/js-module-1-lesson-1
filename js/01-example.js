// Display the total number of apples and grapes on the screen. The difference between apples and grapes.

const apples = Number.parseInt(prompt("Please insert the number of apples"));
const grapes = Number.parseInt(prompt("Please insert the number of grapes"));
const total = apples + grapes;
console.log("total:", total);
const diff = apples - grapes;
console.log("diff:", diff);